# План

Фазы идут по порядку. Каждая заканчивается рабочим состоянием и коммитом. «Вопросы себе» это то, на что я должен уметь ответить после фазы, не заглядывая в документацию. Не могу ответить, значит фаза не закончена.

Где звать агента, отмечено значком 🤖. В остальных местах не звать.

## Фаза 0. Пункт в менюбаре

- [ ] Проект macOS, AppKit, без сториборда, `LSUIElement`, sandbox с сетью.
- [ ] `NSStatusItem` с текстом «F1» и меню из одного пункта «Выйти».
- [ ] swift-format, `.gitignore`, первый коммит.

Вопросы себе: что делает `LSUIElement`? Как приложение без окна вообще работает?

Готово, когда пункт виден и «Выйти» работает.

## Фаза 1. Данные и кэш

- [ ] Подключить F1Kit по версии.
- [ ] `ScheduleState`, `ScheduleCache`, `ScheduleModel` с `refresh()`.
- [ ] Кэш читается при старте, ошибка сети не затирает данные.
- [ ] Тест с `RecordingHTTPClient`: ошибка сохраняет старое состояние.
- [ ] Записать в `NOTES.md` первое, чего не хватило в F1Kit.

Вопросы себе: почему кэш в Application Support, а не в Caches? Что F1Kit должен был бы дать, чтобы этот код стал короче?

## Фаза 2. Текст и меню

- [ ] `StatusTextFormatter` с тестами на все правила из `SPEC.md`.
- [ ] `CountdownTicker` по границе минуты.
- [ ] `MenuBuilder` с тестами, `StatusItemController` собирает меню в `menuWillOpen`.
- [ ] Прошедшие серым, ближайшая жирным, тройка последней гонки.

Вопросы себе: почему текст и меню чистые функции от состояния и времени? Как таймер попадает на границу минуты?

🤖 Ревью. Разделы «Чистые компоненты» и «Строка меню».

## Фаза 3. Обновление по расписанию

- [ ] `RefreshScheduler`: старт, 04:00, пробуждение, два часа после гонки. Тесты «пора ли» и «когда следующий».
- [ ] `NSWorkspace.didWakeNotification` в `AppDelegate`, сброс тикера и проверка обновления.
- [ ] Кнопка «Обновить» с временем в подсказке.
- [ ] Ошибка: серая строка в меню, повтор через час.

Вопросы себе: что происходит с `Timer` во сне? Почему обновление не привязано к тику отсчёта?

## Фаза 4. Уведомления

- [ ] `NotificationIDs`, `NotificationPlanner` с фальшивым центром и тестами.
- [ ] Разрешение при включении напоминаний.
- [ ] Перепланирование при смене состояния и настроек без дублей.
- [ ] Проверка: поставить N так, чтобы уведомление пришло через две минуты.

Вопросы себе: почему идентификаторы детерминированные? Что произойдёт при отзыве разрешения?

🤖 Ревью. Раздел «Уведомления».

## Фаза 5. Настройки и автозапуск

- [ ] `Preferences` с `@Published` поверх `UserDefaults`.
- [ ] `SettingsView` на SwiftUI, `SettingsWindowController`, одно окно, активация.
- [ ] `LaunchAtLogin` через `SMAppService`, статус `requiresApproval` показан.
- [ ] Иконка вместо текста.

Вопросы себе: почему окно не появляется поверх без `activate`? Что такое `requiresApproval`?

## Фаза 6. Неделя в бою

- [ ] Иконка, версия, ссылка на источник в настройках.
- [ ] Смена часового пояса пересчитывает.
- [ ] Прожить неделю: сон, ночь, смена сети. Записать, что сломалось.
- [ ] `NOTES.md`: полный список запросов к F1Kit 0.2.

🤖 Финальное ревью по всему чеклисту.

## Стретч

- [ ] Кэширующий `HTTPClient` как обёртка, чтобы кэш переехал в F1Kit.
- [ ] Меню с зачётом пилотов в подменю.
