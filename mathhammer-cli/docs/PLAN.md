# План

Фазы идут по порядку. Каждая заканчивается рабочим состоянием и коммитом. «Вопросы себе» это то, на что я должен уметь ответить после фазы, не заглядывая в документацию. Не могу ответить, значит фаза не закончена.

Где звать агента, отмечено значком 🤖. В остальных местах не звать.

## Фаза 0. Пакет и Linux

- [ ] `Package.swift`, библиотека и команда, `mathhammer --help` печатает заглушку.
- [ ] `Dockerfile.test`, `swift test` в контейнере.
- [ ] swift-format, `.gitignore`, первый коммит.

Готово, когда собирается на macOS и Linux.

## Фаза 1. Распределения и кубики

- [ ] `Distribution` с `convolve`, `expected`, `probability(atLeast:)`, `percentile`.
- [ ] Тесты: `D6 + D6` даёт известное распределение, ожидание 7; свёртка с константой сдвигает; сумма единица.
- [ ] `Binomial`.
- [ ] `DiceExpression` парсер и распределение, тесты на `D3`, `D6+1`, `2D6`, мусор.

Вопросы себе: почему свёртка это сумма независимых? Что такое обрезка хвостов и когда она портит результат?

🤖 Ревью. Раздел «Вероятности».

## Фаза 2. Попадание и ранение

- [ ] Выписать правила комментариями в `HitRules` и `WoundRules` до кода.
- [ ] Порог, модификаторы с ограничением, естественные 1 и 6.
- [ ] Перебросы трёх видов до модификатора.
- [ ] Таблица ранений, тест на все пять случаев.
- [ ] Тесты таблицей: десять комбинаций с ручным ожиданием.

Вопросы себе: почему переброс до модификатора меняет вероятность иначе, чем после? Что «неудачный» значит при модификаторе?

## Фаза 3. Особые правила

- [ ] Критический порог, Torrent, Blast.
- [ ] Sustained Hits N через свёртку дополнительных попаданий.
- [ ] Lethal Hits как отдельный поток авторанений.
- [ ] Devastating Wounds как урон мимо сейва.
- [ ] Тесты на каждое и на сочетание Sustained плюс Lethal.

Вопросы себе: дополнительные попадания от Sustained могут быть критическими? Что происходит с Lethal при Devastating?

🤖 Ревью. Раздел «Правила».

## Фаза 4. Сейвы, урон, модели

- [ ] `SaveRules` с AP, инвульном, FNP.
- [ ] Урон через свёртку выражения по числу ранений.
- [ ] Убитые модели марковской цепью. Тест: `D6` против моделей с одной раной убивает ровно по одной; урон 2 против модели с 3 ранами: две ранения убивают одну.
- [ ] `Calculator` и `Report`.
- [ ] Десять эталонных профилей в `CalculatorTests`.

Вопросы себе: почему лишний урон пропадает и как это ломает наивную свёртку? Сколько состояний у цепи?

🤖 Ревью. Разделы «Правила» и «Модели».

## Фаза 5. Симулятор и команда

- [ ] `Simulator` с генератором и сидом, независимая реализация.
- [ ] Тест сходимости на пяти профилях.
- [ ] Команда: все флаги, JSON-профиль, таблица, `--json`, `--sim`, коды выхода.
- [ ] Ошибки ввода человеческим языком.

Вопросы себе: что означает расхождение симулятора и точного расчёта: кто прав? Как это выяснить?

🤖 Рутина. Отдать агенту: из десяти эталонных профилей сделать двадцать вариаций с изменённым одним параметром и ожидаемыми значениями, посчитанными симулятором на большом N, по образцу существующих тестов.

## Фаза 6. Релиз

- [ ] `swift build -c release`, бинарник запускается на чистом Mac.
- [ ] CI: macOS и Linux.
- [ ] README с примерами.

🤖 Финальное ревью по всему чеклисту.

## Стретч

- [ ] Homebrew tap и формула.
- [ ] Несколько профилей оружия в одном запуске с суммой.
- [ ] Сравнение двух целей бок о бок.
