# План

Фазы идут по порядку. Каждая заканчивается рабочим состоянием и коммитом. «Вопросы себе» это то, на что я должен уметь ответить после фазы, не заглядывая в документацию. Не могу ответить, значит фаза не закончена.

Где звать агента, отмечено значком 🤖. В остальных местах не звать.

## Фаза 0. Окружение и один запрос

- [ ] `uv init`, зависимости, `ruff` и `mypy` в `pyproject.toml`.
- [ ] `http.Client` с `User-Agent`, один запрос за страницу «3 сентября», вики-текст в консоль.
- [ ] `.gitignore` с `cache/`, первый коммит.

Вопросы себе: что произойдёт без `User-Agent`? Где в ответе лежит вики-текст?

Готово, когда вики-текст дня напечатан.

## Фаза 1. Кэш и все дни

- [ ] `days.all_days()` с названиями страниц, тест на 366 и на «29_февраля».
- [ ] `cache` с тестом на `tmp_path`.
- [ ] `sources.wikitext.fetch_day` с кэшем.
- [ ] Полный прогон с паузой, кэш заполнен. Записать время.

Вопросы себе: почему кэш файл на день, а не один файл? Что будет при Ctrl+C посередине?

## Фаза 2. Разбор и чистка на фикстурах

- [ ] Записать `09-03.wikitext` и ещё два дня в фикстуры.
- [ ] `parse.events_section`, тест на количество кандидатов и первые три.
- [ ] Собрать двадцать разнообразных строк в `lines.txt` с ожидаемым чистым текстом.
- [ ] `clean.clean`, параметризованный тест по строкам.

Вопросы себе: какие три вида разметки оказались самыми частыми? Что `strip_code` не убирает?

🤖 Рутина. Отдать агенту: найти в кэше ещё двадцать строк с редкой разметкой и добавить в `lines.txt` по образцу, с ожидаемым результатом.

## Фаза 3. Фильтр и отбор

- [ ] `filters` со стоп-словами и порогами, тест на каждое правило.
- [ ] `select.pick`, тест на детерминизм и лимит.
- [ ] Прогон по кэшу с выводом статистики отбрасываний по причинам.

Вопросы себе: какая причина отбрасывания самая частая? Не слишком ли строгий фильтр: сколько дней пустых?

🤖 Ревью. Разделы «Чистота» и «Отбор».

## Фаза 4. Вывод и проверка

- [ ] `models.Fact`, `output.validate`, `output.write` атомарно.
- [ ] Тест схемы: 366 ключей, уникальные `id`, непустой текст.
- [ ] Скопировать `facts.json` в календарь, убедиться, что читается.

Вопросы себе: почему пустые дни всё равно присутствуют ключами?

## Фаза 5. Команда целиком

- [ ] Все флаги, коды выхода, `-v`.
- [ ] Повтор на 429 и 5xx, тест с подменённым клиентом.
- [ ] README с примером запуска и упоминанием лицензии.

🤖 Финальное ревью по всему чеклисту.

## Стретч

- [ ] Источник `feed` для английского, сравнить качество.
- [ ] Раздел «Праздники» отдельным флагом.
