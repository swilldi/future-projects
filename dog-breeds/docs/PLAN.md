# План

Фазы идут по порядку. Каждая заканчивается рабочим состоянием и коммитом. «Вопросы себе» это то, на что я должен уметь ответить после фазы, не заглядывая в документацию. Не могу ответить, значит фаза не закончена.

Где звать агента, отмечено значком 🤖. В остальных местах не звать.

## Фаза 0. Каркас и камера

- [ ] Проект iOS, SwiftUI, Swift 6, `NSCameraUsageDescription`.
- [ ] `CameraSession` с превью, разрешение, старт и стоп. Видео на экране.
- [ ] Пакет `Training/` с заглушкой команды.
- [ ] swift-format, `.gitignore` с `Training/data/`, первый коммит.

Вопросы себе: почему сессия на своей очереди? Что произойдёт, если стартовать её на главной?

Готово, когда превью камеры работает на устройстве.

## Фаза 1. Данные

- [ ] Список 30 пород в `Breeds.swift`.
- [ ] `DatasetPreparer`: скачать, распаковать, отобрать, разбить, класс «другое». Пропуск готовых шагов.
- [ ] Тест детерминированности разбивки.
- [ ] Посмотреть глазами по десять снимков пяти пород. Записать, что удивило.

Вопросы себе: почему класс «другое» обязателен? Откуда взять его данные и чтобы они были разнообразными?

## Фаза 2. Обучение

- [ ] `Trainer` на 5 породах по 20 снимков, чтобы проверить конвейер за минуты.
- [ ] Полное обучение на 30 по 100 с аугментациями и сидом. Записать время.
- [ ] Точность топ-1 и топ-3, матрица ошибок худших пар в `NOTES.md`.
- [ ] Экспорт модели с метаданными, копия в `Resources/`. Размер записан.
- [ ] Повторное обучение с тем же сидом: точность в пределах процента.

Вопросы себе: какие пары пород путаются и почему это ожидаемо? Что дали аугментации, сравнить без них?

🤖 Ревью. Раздел «Обучение».

## Фаза 3. Распознавание фото

- [ ] `Classifier` с одним запросом, загрузка модели один раз.
- [ ] `Verdict` и `Thresholds`, тесты на границах.
- [ ] `PhotoClassifyView` через `PhotosPicker`: фото, три породы, вердикт.
- [ ] Пять тестовых картинок в бандле тестов, дымовой тест классификатора.

Вопросы себе: что такое `centerCrop` и как он влияет на собаку у края? Почему кот должен давать «другое», а не «похоже на хаски»?

## Фаза 4. Живая камера

- [ ] Кадры в `AsyncStream` с `bufferingNewest(1)`, ориентация с кадром.
- [ ] `FrameThrottle` с тестами, 200 мс.
- [ ] `CameraViewModel` с циклом, `Smoother`, оверлей.
- [ ] Проверить на живой собаке или на экране с фото. Записать, мигает ли.

Вопросы себе: почему кадры выбрасываются, а не копятся? Как ориентация устройства превращается в ориентацию для Vision?

🤖 Ревью. Разделы «Кадры» и «Изоляция».

## Фаза 5. Коллекция

- [ ] `Sighting`, `SightingStore`, снимок из последнего кадра в JPEG до 1200.
- [ ] `CollectionView`, `SightingDetailView`, удаление.
- [ ] `BreedsView` со счётчиками и фильтром.

## Фаза 6. Нагрев и края

- [ ] `thermalState`: удвоение интервала.
- [ ] Отказ в камере, модель не загрузилась, поворот.
- [ ] Десять минут распознавания: температура, кадры превью.

🤖 Финальное ревью по всему чеклисту.

## Стретч

- [ ] Детекция рамкой через `VNRecognizeObjects` с своей моделью: несколько собак.
- [ ] 60 пород, сравнить точность.
- [ ] Экспорт коллекции.
