# План

Фазы идут по порядку. Каждая заканчивается рабочим состоянием и коммитом. «Вопросы себе» это то, на что я должен уметь ответить после фазы, не заглядывая в документацию. Не могу ответить, значит фаза не закончена.

Где звать агента, отмечено значком 🤖. В остальных местах не звать.

## Фаза 0. Каркас

- [ ] Проект iOS, SwiftUI, Swift 6, GRDB через SPM, фоновый режим location, тексты разрешений.
- [ ] swift-format, `.gitignore`, первый коммит, папки из `ARCHITECTURE.md`.

## Фаза 1. База и расчёты

- [ ] `Walk`, `TrackPoint`, схема GRDB с миграцией и индексом.
- [ ] `WalkRepository` с тестами в памяти: вставка пачкой, точки по прогулке, активная прогулка.
- [ ] `Stats/`: фильтр, дистанция, высота, темп, упрощение. Синтетические треки в тестах.

Вопросы себе: почему `DatabasePool`, а не `DatabaseQueue`? Как гистерезис убирает шум высоты? Что даёт индекс по паре полей?

🤖 Ревью. Разделы «Расчёты» и «База».

## Фаза 2. Геолокация на переднем плане

- [ ] `LocationTracker` с конфигурацией и потоком точек.
- [ ] Разрешение при первом старте, отказ.
- [ ] `WalkRecorder`: старт, точки, пачки, снимки, стоп.
- [ ] `WalkView` с картой, маршрутом, показателями.
- [ ] Прогулка вокруг дома с экраном. Сравнить дистанцию с картами.

Вопросы себе: какие точки отбросил фильтр в реальной прогулке? Сколько их было в начале?

## Фаза 3. Фон и батарея

- [ ] `allowsBackgroundLocationUpdates`, `pausesLocationUpdatesAutomatically = false`.
- [ ] Часовая прогулка с выключенным экраном. Дыры, батарея, записать.
- [ ] Сравнить `Best` и `NearestTenMeters` на одной дороге.
- [ ] Восстановление незавершённой при запуске.
- [ ] Пауза и автопауза.

Вопросы себе: что произошло с треком при блокировке до включения фонового режима? Сколько стоит `Best`?

🤖 Ревью. Раздел «Фон».

## Фаза 4. Живая плашка

- [ ] Таргет виджета, `WalkActivityAttributes`, вью плашки и острова.
- [ ] `LiveActivityController` с троттлингом, тест частоты.
- [ ] `PauseWalkIntent`.
- [ ] Прогулка с плашкой: обновления приходят, кнопка работает.

Вопросы себе: что произойдёт при обновлении каждую секунду? Где выполняется интент?

## Фаза 5. Здоровье

- [ ] `HealthStore`: разрешение при первом сохранении, шаги за интервал, тренировка с маршрутом.
- [ ] `CMPedometer` для живых шагов.
- [ ] Прогулка появилась в Здоровье.
- [ ] iPad без HealthKit не падает.

Вопросы себе: почему разрешение HealthKit не говорит, отказали или нет? Что такое `HKWorkoutRouteBuilder`?

🤖 Ревью. Раздел «Разрешения».

## Фаза 6. История и графики

- [ ] `HistoryView` через `ValueObservation`.
- [ ] `WalkDetailView`: карта, показатели, графики темпа и высоты.
- [ ] `WeeklyStatsView` со столбцами и переключением недель.
- [ ] Удаление.

Вопросы себе: сколько точек можно отдать графику без тормозов? Где упрощать?

## Фаза 7. Края и полировка

- [ ] Туннель, короткая прогулка, «ищем GPS», нет разрешения.
- [ ] Доступность показателей и графиков.
- [ ] Иконка, настройки.

🤖 Финальное ревью по всему чеклисту.

## Стретч

- [ ] Виджет «эта неделя».
- [ ] Приложение на часах: старт и стоп, показатели.
- [ ] Миниатюры карт в истории через `MKMapSnapshotter`.
