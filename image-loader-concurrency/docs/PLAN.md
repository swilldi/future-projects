# План

Фазы идут по порядку. Каждая заканчивается рабочим состоянием и коммитом. «Вопросы себе» это то, на что я должен уметь ответить после фазы, не заглядывая в документацию. Не могу ответить, значит фаза не закончена.

Где звать агента, отмечено значком 🤖. В остальных местах не звать.

## Фаза 0. Каркас

- [ ] Проект iOS, SwiftUI, Swift 6, strict concurrency complete. Thread Sanitizer включён в схеме для Debug.
- [ ] swift-format, `.gitignore`, первый коммит, папки из `ARCHITECTURE.md`.

Готово, когда собирается с санитайзером.

## Фаза 1. Ограничитель и загрузчик на фейке

Цель: вся оркестрация работает и протестирована без сети и без интерфейса.

- [ ] `ImageItem`, `LoaderEvent`, `Downloading`.
- [ ] `ConcurrencyLimiter` актором.
- [ ] Тесты ограничителя: пик, увеличение, уменьшение.
- [ ] `FakeDownloader` с управляемыми продолжениями: тест сам решает, когда загрузка «завершается».
- [ ] `ImageLoader` со `start`, событиями, `defer release`.
- [ ] Тесты: порядок событий, лимит соблюдён при ста элементах.

Вопросы себе: почему `release` в `defer`? Что произойдёт, если задача упадёт до `release` без него? Что такое управляемое продолжение в фейке и зачем оно тестам?

Готово, когда тесты зелёные и детерминированные: прогнать двадцать раз.

🤖 Ревью. Разделы «Отмена и лимит» и «Изоляция».

## Фаза 2. Настоящая загрузка

- [ ] `URLSessionDownloader` через `bytes(for:)`, прогресс, без кэша.
- [ ] `Thumbnailer` через `CGImageSource`.
- [ ] Подключить к загрузчику, запустить в консоли или простом экране, увидеть сто загрузок.

Вопросы себе: где выполняется `Thumbnailer.make` и почему это не блокирует остальные загрузки? Что такое кооперативный пул и сколько в нём потоков?

## Фаза 3. Сетка и состояние

- [ ] `LoaderModel` с `for await events`.
- [ ] `GridView`, `TileView` с пятью состояниями, `StatsView`.
- [ ] Слайдер лимита, применяется на лету.
- [ ] Проверить прокрутку во время загрузки с лимитом 16.

Вопросы себе: почему `LoaderModel` не вызывается из задач напрямую? Что бы сломалось?

## Фаза 4. Отмена и повтор

Цель: узловая фаза проекта.

- [ ] Отменить всё: отмена группы, все ожидающие получают `cancelled` в течение секунды.
- [ ] Отменить одну по тапу. Выбрать схему из `ARCHITECTURE.md`, реализовать, записать, что не сработало.
- [ ] Повторить упавшие: только их, с тем же лимитом.
- [ ] Отмена во время обработки: результат не показывается.
- [ ] Тесты на все четыре сценария с фейком.

Вопросы себе: почему нельзя отменить одного ребёнка `TaskGroup`? Чем отмена неструктурной задачи отличается? Кто проверяет отмену внутри `for await bytes`?

🤖 Ревью. Раздел «Отмена и лимит» повторно, с упором на утечки слотов.

## Фаза 5. Приоритеты и обработка

- [ ] Загрузка с `.userInitiated`, обработка с `.utility`. Замерить, изменилось ли что-то.
- [ ] Попробовать `Task.detached` для обработки, сравнить, вернуть структурный вариант. Записать.
- [ ] Статистика: пик одновременных через событие `concurrency`.

Вопросы себе: когда `Task.detached` оправдан? Что теряется: приоритет, отмена, локальные значения задачи?

## Фаза 6. Лаборатория

- [ ] `RaceDemo` с `nonisolated(unsafe)`: получить меньше ста и отчёт санитайзера.
- [ ] `ActorDemo`: ровно сто.
- [ ] `ReentrancyDemo`: актор с `await` внутри, показать чужое промежуточное состояние.
- [ ] Записать, что поймал компилятор, а что только санитайзер.

Вопросы себе: почему компилятор не поймал гонку в `RaceDemo`? Почему актор не спасает от реентерабельности?

## Фаза 7. Замеры и выводы

- [ ] Таблица в `NOTES.md`: лимит 1, 4, 8, 16 против времени на Wi-Fi и LTE.
- [ ] Ответить на три вопроса из `NOTES.md`.

🤖 Финальное ревью по всему чеклисту.

## Стретч

- [ ] Приоритет видимым плиткам: ограничитель с приоритетной очередью.
- [ ] Вторая схема отмены одной задачи, если первая была неструктурной.
- [ ] `AsyncChannel` из swift-async-algorithms вместо `AsyncStream`, сравнить.
