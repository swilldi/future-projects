# Чеклист ревью

Для агента и для меня. Идти сверху вниз. Проверять только то, что по `PLAN.md` уже должно быть сделано.

## Формат замечания

`[уровень] файл:строка. Что не так. Почему это проблема. Как исправить в одну строку.`

Уровни: блокер (ломает поведение или архитектуру), важно (сработает позже), мелочь (стиль, читаемость). Код в ответе не переписывать. В конце: что сделано хорошо, что проверить руками, что не проверялось.

## Сборка и тесты

- [ ] Без warnings. Strict concurrency без `nonisolated(unsafe)` и `@unchecked Sendable`, исключения только с комментарием, почему.
- [ ] Тесты зелёные. swift-format ничего не меняет.

## Отмена и лимит

- [ ] `release` в `defer` в каждой задаче.
- [ ] Лимит проверен тестом с подсчётом пика.
- [ ] Отмена проверяется перед тяжёлой обработкой.
- [ ] Отмена всего даёт `cancelled` всем ожидающим, слоты не утекают.
- [ ] Повтор не запускает уже готовые.

## Изоляция

- [ ] `ImageLoader` и `ConcurrencyLimiter` акторы, состояние не утекает.
- [ ] Единственный путь в интерфейс это `events`.
- [ ] `nonisolated(unsafe)` и `@unchecked Sendable` только в `Lab/`.
- [ ] `Task.detached` только с комментарием.
- [ ] Ни одного GCD-примитива вне `Lab/`.

## Сеть и обработка

- [ ] Кэш URLSession выключен.
- [ ] Прогресс обрабатывает `-1` в `expectedContentLength`.
- [ ] `Thumbnailer` без UIKit, размер ограничен.
- [ ] Сеть пропала: ошибки, не зависание.

## Доступность

- [ ] Плитки читаются VoiceOver с состоянием.
- [ ] Кнопки с подписями.

## Тесты

- [ ] Покрыто то, что `ARCHITECTURE.md` объявляет тестируемым.
- [ ] Тесты проверяют поведение, не последовательность вызовов.
- [ ] Нет `sleep` и ожиданий по таймеру в тестах.
- [ ] Что не покрыто, записано в `NOTES.md` с причиной.

## Что здесь не проверяем

Дизайн, кэш на диске, поведение в фоне.
