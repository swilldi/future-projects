# Заметки

## Решения

- 2026-09-17. Заготовка создана.
- 2026-09-17. Отмена одной задачи через неструктурные `Task` в словаре под актором. Вторую схему пробуем в стретче.
- 2026-09-17. Обработка картинки в структурной задаче, не в `Task.detached`.
## Открытые вопросы

- Буфер `AsyncStream`: без ограничения или `.bufferingNewest(100)`?
- `CGImage` через `await`: передавать `Data` и декодировать на главном или помечать?

## Идеи на потом

- Тот же загрузчик как SPM-пакет для других проектов.
- Сравнить с `OperationQueue` на отдельной ветке, просто чтобы увидеть разницу.

## Что не покрыто тестами и почему

Заполнить по ходу.

## Ссылки

- picsum.photos.
