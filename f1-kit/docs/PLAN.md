# План

Фазы идут по порядку. Каждая заканчивается рабочим состоянием и коммитом. «Вопросы себе» это то, на что я должен уметь ответить после фазы, не заглядывая в документацию. Не могу ответить, значит фаза не закончена.

Где звать агента, отмечено значком 🤖. В остальных местах не звать.

## Фаза 0. Пакет и фикстуры

- [ ] `Package.swift` с таргетами `F1Kit`, `F1KitTests`, `f1`. Платформы iOS и macOS.
- [ ] Скачать реальные ответы Jolpica за прошлый сезон в `Fixtures/`: расписание, результаты одной гонки, оба зачёта.
- [ ] Записать лимиты Jolpica в `NOTES.md`.
- [ ] swift-format, `.gitignore`, `Dockerfile.test`, первый коммит. `swift build` на macOS и в Docker.

Вопросы себе: чем продукт отличается от таргета? Почему фикстуры записываются до DTO?

Готово, когда пакет собирается на обеих платформах.

## Фаза 1. DTO, модели, маппинг

- [ ] `ErgastEnvelope`, DTO по фикстурам.
- [ ] Публичные модели, `F1Error`.
- [ ] `ErgastMapper` с разбором дат.
- [ ] Тесты маппинга: уикенд со спринтом даёт семь сессий по порядку, без спринта пять, отсутствующее время у гонки даёт ошибку с путём.

Вопросы себе: почему модели плоские, а не повторяют DTO? Что несёт `codingPath`?

🤖 Ревью. Разделы «Публичный API» и «Маппинг».

## Фаза 2. Транспорт

- [ ] `HTTPClient`, `URLSessionHTTPClient` с конфигурацией в init.
- [ ] `Endpoint`, `ErgastEndpoints`.
- [ ] `Sleeper`, `RetryPolicy` с тестами таблицей.
- [ ] `Transport` с повторами. Тесты с `RecordingHTTPClient` и `InstantSleeper`: 500 трижды, 429 с `Retry-After`, 404 сразу, сетевая ошибка.
- [ ] `Interceptor`, `LoggingInterceptor`, `RateLimitInterceptor` с тестом задержек.

Вопросы себе: почему цикл повторов в одном месте? Что произойдёт при отмене задачи во время сна между попытками? Почему лимит частоты это актор?

🤖 Ревью. Раздел «Транспорт».

## Фаза 3. Клиент

- [ ] `F1Client` с четырьмя методами и `Configuration`.
- [ ] `URLProtocolStub`, тесты клиента на фикстурах целиком.
- [ ] `nextSession` с тестами на фиксированной дате: середина сезона, до сезона, после.
- [ ] Проверить тесты в Docker. Если `URLProtocol` не работает на Linux, ограничить и записать.

Вопросы себе: что тестирует `URLProtocol`, чего не тестирует `RecordingHTTPClient`? Почему `nextSession` принимает дату, а не берёт текущую?

## Фаза 4. Команда

- [ ] `f1 next`, `f1 schedule`, `f1 standings` с ArgumentParser.
- [ ] Локальное время, таблицы.
- [ ] Запустить с реальным API. Дымовой тест в CI с сетью, помеченный как необязательный.

Вопросы себе: почему команда, а не тестовое приложение? Что она проверяет?

## Фаза 5. Документация, CI, версия

- [ ] `///` на всём публичном. DocC с обзором и туториалом «Следующая сессия за пять минут».
- [ ] GitHub Actions: матрица macOS и Linux, `swift test`, сборка DocC.
- [ ] README с примером в три строки.
- [ ] Тег `0.1.0`. Подключить в пустой проект по URL, убедиться.
- [ ] `NOTES.md`: список решений по API и что считаю ломающим изменением.

Вопросы себе: что в моём API я бы захотел поменять через месяц и почему это будет `1.0.0`?

🤖 Финальное ревью по всему чеклисту. Отдельно спросить: какие `public` можно было бы спрятать.

## Стретч

- [ ] Второй провайдер OpenF1 за тем же `F1Client` через протокол провайдера.
- [ ] `AsyncSequence` обновлений расписания с интервалом.
- [ ] Вынести `Transport/` в отдельный пакет и подключить обратно.
