# План

Фазы идут по порядку. Каждая заканчивается рабочим состоянием и коммитом. «Вопросы себе» это то, на что я должен уметь ответить после фазы, не заглядывая в документацию. Не могу ответить, значит фаза не закончена.

Где звать агента, отмечено значком 🤖. В остальных местах не звать.

## Фаза 0. Каркас

- [ ] Проект iOS, SwiftUI, Swift 6, strict concurrency.
- [ ] GRDB через SPM, версия закреплена.
- [ ] swift-format, `.gitignore`, первый коммит. Папки из `ARCHITECTURE.md`.

Готово, когда собирается.

## Фаза 1. Домен, протокол, экраны на памяти

Цель: приложение работает на реализации в памяти, контрактные тесты написаны.

- [ ] `Note`, `NotesRepository`, `NoteSeeder` со словарём слов.
- [ ] `InMemoryNotesRepository` как шестая, только для тестов и первого запуска.
- [ ] `NotesRepositoryContractTests`: сохранить и прочитать; поиск по слову; фильтр по тегу; удаление; `changes` срабатывает после записи; `saveBatch` тысячи; пустой заголовок даёт «Без названия».
- [ ] Список, редактор, настройки с переключателем.

Вопросы себе: почему `changes` это `Void`, а не поток заметок? Что контрактный тест гарантирует, а что нет?

Готово, когда контракт зелёный на памяти и экраны работают.

🤖 Ревью. Разделы «Контракт» и «Домен».

## Фаза 2. Файлы и UserDefaults

- [ ] `FileNotesRepository` актором, файл на заметку, атомарная запись.
- [ ] `UserDefaultsNotesRepository`.
- [ ] Обе проходят контракт.
- [ ] Засеять тысячу в каждое, открыть список, почувствовать.

Вопросы себе: что делает `UserDefaults` при каждом `set`? Почему файл на заметку, а не один файл на все?

## Фаза 3. CoreData

- [ ] Модель `NoteEntity`, стек, фоновый контекст.
- [ ] Маппинг внутри `perform`, `NSManagedObject` не выходит наружу.
- [ ] Поиск через `NSPredicate` с `CONTAINS[cd]`.
- [ ] `changes` из уведомления о сохранении.
- [ ] Контракт зелёный.

Вопросы себе: сколько контекстов и зачем каждый? Что такое `objectID` и когда он нужен? Почему нельзя читать сущность из другого потока?

🤖 Ревью. Раздел «CoreData».

## Фаза 4. SwiftData

- [ ] `NoteModel` с `@Model`, контейнер в init.
- [ ] `@ModelActor` для записи.
- [ ] Поиск через `#Predicate`.
- [ ] Контракт зелёный.
- [ ] Записать, что SwiftData делает проще CoreData, а что не даёт сделать вообще.

Вопросы себе: где под SwiftData торчит CoreData? Что нельзя выразить в `#Predicate`?

## Фаза 5. GRDB

- [ ] Схема: таблицы `note`, `tag`, `note_tag`. `DatabaseMigrator`.
- [ ] `NoteRecord`, запись и чтение в транзакциях.
- [ ] Поиск через `LIKE`, потом FTS5. Замерить оба.
- [ ] `ValueObservation` в `changes`.
- [ ] Контракт зелёный.

Вопросы себе: зачем три таблицы вместо JSON-столбца с тегами? Что FTS5 делает при вставке? Что такое `DatabaseQueue` против `DatabasePool`?

🤖 Ревью. Раздел «GRDB».

## Фаза 6. Замеры

- [ ] `BenchmarkRunner`: семь операций, три прогона, медиана.
- [ ] Экран замеров с таблицей и копированием.
- [ ] Прогнать на устройстве, не в симуляторе. Таблицу в `NOTES.md`.
- [ ] Три вывода по таблице, записать.

Вопросы себе: почему на устройстве, а не в симуляторе? Что медленнее всего и почему именно оно?

## Фаза 7. Миграция

- [ ] `isPinned` в `Note`, закреплённые сверху в списке.
- [ ] Миграция в каждом из пяти на засеянных данных. Проверить руками: засеять на старой версии, обновить, открыть.
- [ ] Тест миграции на каждое: файл или база старого формата в фикстурах читается новой версией.
- [ ] Записать, где миграция была бесплатной, а где потребовала кода.

Вопросы себе: что произойдёт в CoreData без версии модели? В SwiftData без `VersionedSchema`? В GRDB без миграции?

🤖 Финальное ревью по всему чеклисту.

## Фаза 8. Выводы

- [ ] В `NOTES.md`: что взял бы для реального проекта в трёх ситуациях: маленькое приложение, синхронизация с iCloud, сложные запросы.

## Стретч

- [ ] Отношение «заметка принадлежит папке» во всех пяти, чтобы увидеть, где связи удобны.
- [ ] `DatabasePool` в GRDB и параллельное чтение.
