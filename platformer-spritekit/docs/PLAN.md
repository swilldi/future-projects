# План

Фазы идут по порядку. Каждая заканчивается рабочим состоянием и коммитом. «Вопросы себе» это то, на что я должен уметь ответить после фазы, не заглядывая в документацию. Не могу ответить, значит фаза не закончена.

Где звать агента, отмечено значком 🤖. В остальных местах не звать.

## Фаза 0. Сцена

- [ ] Проект iOS, SwiftUI, Swift 6. `SpriteView` с пустой сценой и фоном.
- [ ] Ассеты: набор тайлов и герой из свободного пака, лицензия в `NOTES.md`.
- [ ] swift-format, `.gitignore`, первый коммит.

Готово, когда сцена рисует фон и один спрайт.

## Фаза 1. Уровень из текста

- [ ] `LevelModel`, `LevelLoader` с тестами: валидная карта, разная длина строк, нет старта, неизвестный символ.
- [ ] `level1.txt`.
- [ ] `GameScene` строит `SKTileMapNode` земли и узлы объектов из модели.
- [ ] Камера показывает старт.

Вопросы себе: почему уровень это текст, а не редактор Xcode? Как тайл-карта работает с физикой?

🤖 Ревью. Раздел «Чистые типы».

## Фаза 2. Бег и прыжок числами

- [ ] `InputState`, экранные кнопки.
- [ ] `PlayerMovement` с параметрами из `SPEC.md`.
- [ ] Тесты: высота при удержании три тайла, при отпускании полтора, coyote 6 кадров да, 7 нет, буфер срабатывает.
- [ ] `PlayerControlComponent`, кинематическое тело, `isGrounded` счётчиком.
- [ ] Побегать. Подкрутить параметры, записать итоговые.

Вопросы себе: почему движение не через физическое тело? Что такое coyote time и почему без него игра ощущается несправедливой?

🤖 Ревью. Раздел «Движение».

## Фаза 3. Сущности и состояния

- [ ] Сущности и компоненты через GameplayKit для героя, врага, платформы.
- [ ] `GameStateMachine` с тестами переходов.
- [ ] Пауза при уходе в фон.

Вопросы себе: что дал ECS на трёх типах сущностей? Когда он бы окупился?

## Фаза 4. Камера и монеты

- [ ] `CameraController` с мёртвой зоной и границами.
- [ ] Монеты: категория, контакт, счётчик, HUD, звук.
- [ ] Прогрев звука.

Вопросы себе: почему камера с мёртвой зоной, а не привязана к герою?

## Фаза 5. Враги и смерть

- [ ] `PatrolComponent`: разворот у края и стены.
- [ ] Контакт с врагом: сверху давим, сбоку умираем.
- [ ] Пропасть как сенсор, жизни, рестарт, экран «Заново».
- [ ] Анимация смерти, `Dying` и `Respawning`.

Вопросы себе: как определить «сверху» по контакту? Что такое `contactNormal`?

🤖 Ревью. Раздел «Контакты».

## Фаза 6. Платформы и флаг

- [ ] Платформы `=` проходимые снизу через смену маски по направлению.
- [ ] Движущиеся платформы с переносом героя.
- [ ] Флаг, состояние `Won`, экран победы.

Вопросы себе: почему герой скользит с движущейся платформы без переноса?

## Фаза 7. Геймпад и полировка

- [ ] `GCController`: подключение, отключение, кнопки на экране прячутся.
- [ ] Тест `jumpJustPressed` один кадр.
- [ ] 60 кадров на устройстве, Instruments: нет аллокаций в `update`.
- [ ] Дать поиграть кому-то, записать, где прыжок не ощущается.

🤖 Финальное ревью по всему чеклисту.

## Стретч

- [ ] Второй уровень и выбор.
- [ ] Анимации бега и прыжка из атласа.
- [ ] Параллакс-фон.
