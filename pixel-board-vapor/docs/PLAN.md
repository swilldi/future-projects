# План

Фазы идут по порядку. Каждая заканчивается рабочим состоянием и коммитом. «Вопросы себе» это то, на что я должен уметь ответить после фазы, не заглядывая в документацию. Не могу ответить, значит фаза не закончена.

Где звать агента, отмечено значком 🤖. В остальных местах не звать.

## Фаза 0. Три пакета и Docker

- [ ] `PixelShared` с сообщениями и `Board`, тесты кодирования.
- [ ] `Server` из шаблона Vapor, `/healthz`, зависимость от `PixelShared`.
- [ ] `Client` пустое приложение с зависимостью от `PixelShared`.
- [ ] `Dockerfile`, `compose.yaml` с Postgres, сервер отвечает из контейнера.
- [ ] swift-format, `.gitignore`, первый коммит.

Вопросы себе: как локальный пакет подключается к Vapor-проекту и к Xcode-проекту одновременно? Что из Foundation нельзя использовать в общем пакете?

Готово, когда оба проекта собираются с общим пакетом и контейнер отвечает.

## Фаза 1. Доска в памяти

- [ ] `BoardState` актор с тестами.
- [ ] `GET /board` через XCTVapor.

## Фаза 2. WebSocket и хаб

- [ ] `LiveHub` с фейковыми соединениями: тесты рассылки, задержки, удаления.
- [ ] `LiveController`: `hello`, `place`, закрытие на мусор.
- [ ] Пинг и удаление мёртвых.
- [ ] Проверить двумя `wscat` или подобным.

Вопросы себе: почему задержка проверяется на сервере? Что произойдёт, если один клиент перестанет читать сообщения?

🤖 Ревью. Разделы «Акторы» и «Протокол».

## Фаза 3. История и снимки

- [ ] Модели Fluent, миграции, команда миграции в compose.
- [ ] `EventLog`: пачки раз в секунду, снимок раз в минуту, лимит буфера. Тесты с фейком.
- [ ] Загрузка при старте: снимок плюс события.
- [ ] Перезапустить сервер, доска на месте.

Вопросы себе: почему события и снимки, а не таблица пикселей? Что произойдёт при падении между снимками?

🤖 Ревью. Раздел «База».

## Фаза 4. Клиент: холст и снимок

- [ ] `BoardAPI`, `BoardModel`, `CanvasView` с зумом и панорамой.
- [ ] Палитра, тап переводится в координаты.
- [ ] Снимок с сервера рисуется.

Вопросы себе: как перевести точку касания в пиксель при масштабе и смещении? Сколько стоит перерисовать 10 000 пикселей в `Canvas`?

## Фаза 5. Клиент: живое соединение

- [ ] `LiveConnection` актор, `hello`, поток сообщений.
- [ ] Оптимизм, откат, подтверждение в `BoardModel`, тесты.
- [ ] Таймер задержки.
- [ ] Переподключение с задержкой, снимок заново, индикатор.
- [ ] Два телефона видят друг друга.

Вопросы себе: почему после переподключения снимок обязателен? Как `pending` защищает от двойного применения?

🤖 Ревью. Раздел «Клиент».

## Фаза 6. Нагрузка

- [ ] `LoadTest`: сто клиентов, полсекунды между пикселями, десять минут.
- [ ] Память и процессор сервера в контейнере, записать.
- [ ] Медленный клиент: один не читает, остальные не страдают.
- [ ] Исправить, что нашлось.

Вопросы себе: где было узкое место: рассылка, актор, база? Как это увидели?

## Фаза 7. Тесты, края, выводы

- [ ] XCTVapor на маршруты и рукопожатие.
- [ ] База недоступна во время работы: буфер, лог, восстановление.
- [ ] `NOTES.md`: сравнение с Go по пяти пунктам.

🤖 Финальное ревью по всему чеклисту.

## Стретч

- [ ] Бинарный протокол вместо JSON, сравнить размер снимка.
- [ ] Таймлапс из событий.
- [ ] Развернуть на Fly.io или аналоге.
