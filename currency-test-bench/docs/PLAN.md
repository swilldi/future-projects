# План

Фазы идут по порядку. Каждая заканчивается рабочим состоянием и коммитом. «Вопросы себе» это то, на что я должен уметь ответить после фазы, не заглядывая в документацию. Не могу ответить, значит фаза не закончена.

Где звать агента, отмечено значком 🤖. В остальных местах не звать.

## Фаза 0. Каркас и пустой CI

Цель: проект собирается, CI запускает один пустой тест и зелёный.

- [ ] Проект iOS, SwiftUI, Swift 6, strict concurrency.
- [ ] Четыре тестовых таргета: `UnitTests` на Swift Testing, `SnapshotTests`, `UITests`, `PerformanceTests` на XCTest.
- [ ] swift-format, `.gitignore`, первый коммит.
- [ ] GitHub Actions: workflow на push, `xcodebuild test` для `UnitTests` на симуляторе. Зелёный.

Вопросы себе: почему UI-тесты нельзя написать на Swift Testing? Что делает `xcodebuild test -only-testing`?

Готово, когда на GitHub зелёная галочка.

## Фаза 1. Движок, тест первым

Цель: `ConversionEngine` написан через TDD: сначала падающий тест, потом код.

- [ ] Тест: конвертация EUR в USD по известному курсу даёт ожидаемое.
- [ ] Тест: одинаковые валюты дают сумму.
- [ ] Тест: нет курса даёт ошибку `missingRate`.
- [ ] Параметризованный тест на десять пар с известными результатами.
- [ ] Тест на округление: 1/3 даёт 0,33, банковское округление на 0,125.
- [ ] Только теперь `ConversionEngine`.
- [ ] Замер: `measure` на 10 000 конвертаций, записать число.

Вопросы себе: что даёт написание теста до кода, кроме дисциплины? Как выглядит тест, который проверяет реализацию, а не поведение?

Готово, когда все тесты движка зелёные и написаны раньше кода по истории коммитов.

🤖 Ревью. Разделы «Движок» и «Тесты». Отдельно спросить: какие тесты проверяют реализацию вместо поведения.

## Фаза 2. Сеть и кэш через подмену

Цель: клиент и репозиторий покрыты интеграционными тестами без сети.

- [ ] `URLProtocolStub` с очередью ответов: статус, данные, ошибка.
- [ ] `FrankfurterClient` с `URLSessionConfiguration` в init.
- [ ] Тесты клиента: правильный URL, декодирование фикстуры, ошибка на 500, ошибка на битом JSON.
- [ ] `RatesCache` на файле и `InMemoryRatesCache` для тестов.
- [ ] `RatesRepository` с подменяемыми часами.
- [ ] Тесты репозитория: свежий кэш не ходит в сеть; старый ходит; сеть упала, кэш есть, флаг «устарело»; сеть упала, кэша нет, ошибка.

Вопросы себе: чем `URLProtocol` лучше мока `RatesClient`? Что он ловит, чего мок не поймает? Зачем подменять часы?

Готово, когда репозиторий покрыт на все четыре ветки.

## Фаза 3. ViewModel и экран

Цель: экран работает, ViewModel покрыт.

- [ ] `ConverterViewModel` с фиксированной локалью в init.
- [ ] Тесты ViewModel: ввод «1,5» даёт результат; буквы не ломают; смена валюты пересчитывает; нет курса даёт сообщение.
- [ ] `ConverterView`, `CurrencyPickerView`.
- [ ] `AppDependencies` и `LaunchArguments`.

Вопросы себе: почему локаль передаётся в init, а не берётся из системы? Что будет с тестами на CI с другой локалью?

🤖 Ревью. Разделы «Границы» и «Тесты».

## Фаза 4. Снапшоты

Цель: три состояния экрана зафиксированы картинками.

- [ ] Подключить swift-snapshot-testing к `SnapshotTests`.
- [ ] Снапшоты: обычное состояние, «курсы устарели», «курсы недоступны». Одно устройство, записано в `NOTES.md`.
- [ ] Снапшот на самом большом Dynamic Type.
- [ ] Сломать вёрстку на один пиксель, увидеть падение, починить.

Вопросы себе: когда снапшот-тест вреден? Что делать, когда дизайн меняется намеренно?

Готово, когда снапшоты в репозитории и зелёные.

## Фаза 5. UI-тесты

Цель: сценарий целиком проходит стабильно.

- [ ] `FixtureRatesClient` и обработка `-uiTesting` в `AppDependencies`.
- [ ] Отключение анимаций по аргументу.
- [ ] Идентификаторы доступности на поле, пикерах, результате.
- [ ] Тест: ввести 100, выбрать USD и EUR, проверить результат по фикстуре.
- [ ] Тест: `-uiTestingNoRates` показывает «курсы недоступны».
- [ ] Прогнать десять раз подряд. Если хоть раз упал, чинить, не перезапускать.

Вопросы себе: почему UI-тесты падают «иногда»? Какие три причины и как каждая лечится?

🤖 Рутина. Отдать агенту разметку `accessibilityIdentifier` на всех элементах по образцу первых трёх.

## Фаза 6. CI целиком и покрытие

- [ ] Workflow: swift-format lint, юниты и снапшоты на каждый push, UI-тесты только на main.
- [ ] Покрытие включено, отчёт в артефактах.
- [ ] Бейдж в README.
- [ ] Таблица в `NOTES.md`: вид теста, количество, время, что поймал.

Вопросы себе: почему UI-тесты не на каждый push? Сколько стоит минута macOS-раннера?

## Фаза 7. Проверка тестов

Цель: убедиться, что тесты ловят.

- [ ] Сломать движок пятью разными способами по одному: знак, порядок валют, округление, курс EUR, обработка отсутствия. Записать, какие тесты упали на каждый.
- [ ] Если какой-то способ не поймался, добавить тест.

Вопросы себе: что такое мутационное тестирование и почему я сделал его руками?

🤖 Финальное ревью по всему чеклисту.

## Стретч

- [ ] Тест на утечку памяти ViewModel через `weak` и `#expect(nil)` после выхода из области.
- [ ] Поддержка валют без дробной части.
