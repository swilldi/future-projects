# Страны: MVVM на AppKit

Полигон 10 из 10. Одно и то же приложение «Страны», десять раскладок кода. Здесь: **MVVM с Combine, AppKit, macOS**.

Тот же MVVM с Combine, что в третьем полигоне, но на macOS и AppKit. ViewModel те же по смыслу, а view это `NSSplitViewController` с тремя панелями, `NSTableView`, тулбар и меню. Единственный полигон, где меняется не архитектура, а платформа.

## Зачем

Понять, что в архитектуре зависит от UI-фреймворка, а что нет, на самом близком сравнении: те же ViewModel, другая платформа. И заодно потрогать AppKit живьём: окна, панели, тулбар, меню, responder chain, которых в iOS нет.

## Что хочу понять

- `NSSplitViewController` с боковой панелью, списком и деталями.
- `NSTableView` с diffable data source и view-based ячейками.
- `NSToolbar`, `NSSearchToolbarItem`, меню и responder chain: команда ⌘R, которая доходит до нужного контроллера.
- Окно как координатор: кто связывает три панели.
- App Sandbox и почему без entitlement сеть молчит.
- Что из UIKit-привычек не работает и наоборот.

## Что делает приложение

Справочник стран: список с флагом и столицей, поиск, обновление из сети, детали с кнопкой «в избранное», список избранного, кэш для запуска без сети. Полное ТЗ в `docs/SPEC.md`, оно одинаково для всех десяти полигонов.

## Готово, когда

- Три экрана работают на устройстве, запуск без сети с кэшем показывает список, избранное переживает перезапуск.
- То, что эта архитектура объявляет тестируемым (см. `docs/ARCHITECTURE.md`), покрыто тестами.
- Агент прошёл по `docs/REVIEW.md`, блокеров нет.
- В корневом индексе заполнена строка сравнения, а в `docs/NOTES.md` ответы на вопросы из раздела «Что записать в сравнение».

## Как начать

1. `docs/SPEC.md`: прочитать целиком, если это первый полигон, иначе пробежать, оно не менялось.
2. `docs/ARCHITECTURE.md`: прочитать целиком. Это единственное, что здесь отличается от соседей.
3. `docs/STACK.md`: раздел «что почитать» перед соответствующей фазой.
4. `docs/PLAN.md`: идти по фазам. `Services/` можно скопировать из предыдущего полигона.
5. `docs/NOTES.md`: по ходу записывать, что бесит и что нравится. Это и есть результат.
