# План

Фазы идут по порядку. Каждая заканчивается рабочим состоянием и коммитом. «Вопросы себе» это то, на что я должен уметь ответить после фазы, не заглядывая в документацию. Не могу ответить, значит фаза не закончена.

Где звать агента, отмечено значком 🤖. В остальных местах не звать.

## Фаза 0. Каркас и чистый экран

- [ ] Проект iOS, SwiftUI, Swift 6, strict concurrency. `CADisableMinimumFrameDurationOnPhone` в Info.plist.
- [ ] swift-format, `.gitignore`, первый коммит, папки из `ARCHITECTURE.md`.
- [ ] `MetalView` с `MTKView`, `Renderer` очищает экран цветом. 120 кадров на счётчике.

Вопросы себе: кто зовёт `draw(in:)` и на каком потоке? Что такое `currentRenderPassDescriptor` и почему он может быть nil?

Готово, когда экран залит и счётчик показывает 120 на ProMotion.

## Фаза 1. Треугольник

Цель: полный конвейер от буфера до пикселя.

- [ ] `ShaderTypes.h`, bridging header, `LineVertex`.
- [ ] Вершинный и фрагментный шейдеры, конвейер, буфер с тремя вершинами, `draw`.
- [ ] Поменять цвет через юниформ.

Вопросы себе: что такое NDC? Куда пропал треугольник, если я перепутал порядок вершин? Как шейдер узнаёт, откуда читать буфер?

Готово, когда треугольник на экране и цвет меняется из Swift.

🤖 Ревью. Раздел «Структуры и буферы».

## Фаза 2. Линии на CPU

- [ ] `WaveSimulator` с тестами: периодичность, ограничение амплитуды.
- [ ] Генерация полосы треугольников для восьми линий с толщиной на CPU.
- [ ] Буфер вершин, `draw` полосой. Линии колышутся.
- [ ] Свечение по краю через `edge` во фрагментном шейдере.

Вопросы себе: сколько вершин на линию и сколько байт в кадр? Что такое `triangleStrip` и почему стыки между линиями надо разрывать?

## Фаза 3. Кадры в полёте

- [ ] `FrameBuffers` с тремя наборами, семафор.
- [ ] Обработчик завершения, индекс по кругу.
- [ ] Убрать семафор, увидеть артефакты, вернуть. Записать, что видел.

Вопросы себе: что произойдёт с одним набором буферов? Почему три, а не два?

🤖 Ревью. Раздел «Кадры в полёте».

## Фаза 4. Волна в шейдере

- [ ] В буфере только номер линии и параметр x, форма считается в вершинном шейдере по `vertex_id`.
- [ ] Юниформы: время, амплитуда, скорость, толщина.
- [ ] Сравнить время кадра с фазой 2, записать.
- [ ] `WaveSimulator` остаётся для тестов и как эталон: проверить руками, что картинка та же.

Вопросы себе: что переехало с CPU на GPU и сколько байт в кадр стало? Почему нормаль для толщины в шейдере считать сложнее?

## Фаза 5. Частицы

- [ ] `ParticleSystem` с тестами, три стиля.
- [ ] `ParticleInstance`, буфер экземпляров, конвейер с альфа-смешиванием.
- [ ] Один квадрат, `instanceCount`.
- [ ] Слайдер количества, пикер стиля.

Вопросы себе: что такое инстансинг и что было бы без него? Почему альфа-смешивание требует порядок рисования?

## Фаза 6. Касания и панель

- [ ] До четырёх точек ряби в юниформах, затухание.
- [ ] Гауссов бугор в шейдере линий.
- [ ] Панель слайдеров, кнопка скрыть.
- [ ] MSAA 4x.

Вопросы себе: почему рябь в юниформах, а не в отдельном буфере? Что стоит MSAA по времени кадра?

## Фаза 7. Измерения

- [ ] Захват кадра: два прохода, размеры буферов, время каждого `draw`.
- [ ] Instruments: ноль аллокаций в `draw(in:)`.
- [ ] Таблица в `NOTES.md`: время кадра при 1000, 5000, 20000 частиц; CPU-волна против шейдерной; с MSAA и без.
- [ ] Уход в фон и возврат: без скачка времени.

Вопросы себе: где узкое место при 20000 частиц: CPU-обновление или GPU? Как это видно в захвате?

🤖 Финальное ревью по всему чеклисту.

## Стретч

- [ ] Вычислительный шейдер обновляет частицы на GPU, CPU только пишет параметры. Сравнить с таблицей.
- [ ] Свечение по-настоящему: второй проход с размытием в текстуру.
