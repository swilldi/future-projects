# План

Фазы идут по порядку. Каждая заканчивается рабочим состоянием и коммитом. «Вопросы себе» это то, на что я должен уметь ответить после фазы, не заглядывая в документацию. Не могу ответить, значит фаза не закончена.

Где звать агента, отмечено значком 🤖. В остальных местах не звать.

## Фаза 0. Каркас на двух платформах

- [ ] Проект многоплатформенный, iOS и macOS, SwiftUI, Swift 6.
- [ ] SwiftData локально, без CloudKit, контейнер в памяти для превью.
- [ ] swift-format, `.gitignore`, первый коммит.

Готово, когда пустое приложение запускается на iPhone и Mac.

## Фаза 1. Модель и ISBN

- [ ] `Book` по `SPEC.md`, с оглядкой на ограничения CloudKit.
- [ ] `ISBN` с тестами: валидные 10 и 13, X, дефисы, мусор, конвертация.
- [ ] `BookRepository` с тестами на контейнере в памяти.
- [ ] Полки с `@Query`, ручное добавление через форму.

Вопросы себе: какие три ограничения CloudKit накладывает на модель и почему? Почему уникальность ISBN не ограничением?

🤖 Ревью. Разделы «Модель» и «ISBN».

## Фаза 2. Метаданные

- [ ] Записать ответы Open Library и Google Books для пяти книг в фикстуры.
- [ ] `MetadataProvider`, оба провайдера, тесты маппинга.
- [ ] `MetadataService` с цепочкой и уменьшением обложки, тесты с фейками.
- [ ] Ввод ISBN, превью, «на полку».

Вопросы себе: почему цепочка, а не выбор источника пользователем? Что происходит при редиректе Open Library?

## Фаза 3. Экраны

- [ ] `ShelvesView` с полками, поиском, сортировкой.
- [ ] `BookDetailView` с правками напрямую в модель.
- [ ] Дубликат ISBN: предупреждение и переход.
- [ ] Заглушка обложки.

Вопросы себе: почему у деталей нет ViewModel? Как `@Bindable` пишет в SwiftData?

🤖 Ревью. Раздел «Данные».

## Фаза 4. Сканер

- [ ] `ScannerView` с EAN-13, `NSCameraUsageDescription`.
- [ ] Автоподтверждение после секунды стабильного распознавания.
- [ ] Не EAN-13: подсказка.
- [ ] Отсканировать двадцать книг, записать, где споткнулось.

Вопросы себе: что такое символология? Почему сканер не работает в симуляторе?

## Фаза 5. CloudKit

- [ ] Контейнер в консоли, entitlements, `ModelConfiguration` с CloudKit.
- [ ] Первый запуск с непустой базой: проверить, что данные ушли.
- [ ] Два устройства: добавить на одном, увидеть на другом.
- [ ] Офлайн-правки на обоих, сходимость.
- [ ] Без аккаунта iCloud: локальная работа, подсказка в настройках.

Вопросы себе: что произошло бы с моделью с `.unique` при включении CloudKit? Где посмотреть данные в консоли?

🤖 Ревью. Раздел «CloudKit».

## Фаза 6. Mac

- [ ] `PlatformLayout`: боковая панель на Mac, вкладки на iPhone.
- [ ] Тулбар и команды меню на Mac: добавить ⌘N, поиск ⌘F.
- [ ] Кнопка сканирования отсутствует на Mac.
- [ ] Посчитать строки под `#if os`, записать.

Вопросы себе: сколько кода общего? Что заставило ветвиться?

## Фаза 7. Полировка

- [ ] Пустые состояния, ошибки сети, удаление с подтверждением.
- [ ] Доступность: обложки с описанием, звёзды как слайдер.
- [ ] Иконка, о приложении.

🤖 Финальное ревью по всему чеклисту.

## Стретч

- [ ] Swift Charts: прочитано по годам.
- [ ] Обложка из фото через `PhotosPicker`.
- [ ] Виджет «сейчас читаю».
