# План

Фазы идут по порядку. Каждая заканчивается рабочим состоянием и коммитом. «Вопросы себе» это то, на что я должен уметь ответить после фазы, не заглядывая в документацию. Не могу ответить, значит фаза не закончена.

Где звать агента, отмечено значком 🤖. В остальных местах не звать.

## Фаза 0. Скелет из сокращалки

- [ ] Скопировать раскладку, конфиг, middleware, Dockerfile, compose, Makefile, CI.
- [ ] `/healthz` работает из контейнера.
- [ ] Записать в `NOTES.md`, что скопировано без изменений.

Готово, когда контейнер отвечает.

## Фаза 1. Схема и импорт

- [ ] Миграции: таблица, последовательность, триггер ревизии, индексы.
- [ ] `facts.Fact`, `Store`, `Service` с валидацией.
- [ ] `postgres.Store` чтение по дате и случайный.
- [ ] `Importer` с условным upsert, `factsctl import`.
- [ ] Интеграционные тесты: триггер даёт ревизию, импорт дважды даёт ноль обновлений.

Вопросы себе: почему ревизия в триггере, а не в коде? Как условный upsert не трогает ревизию у неизменившихся?

🤖 Ревью. Разделы «Ревизии» и «Импорт».

## Фаза 2. Чтение и ETag

- [ ] Обработчики по дате и случайный, `memstore`, тесты.
- [ ] ETag middleware, `If-None-Match`, 304, `Cache-Control`.
- [ ] Тесты: совпадение даёт 304 без тела, несовпадение 200 с новым ETag.

Вопросы себе: почему ETag из ревизии, а не хэш тела? Что делает промежуточный кэш с `public, max-age`?

## Фаза 3. Дельта

- [ ] `Delta` со `since`, `limit`, `hasMore`, надгробиями.
- [ ] `pg_advisory_lock` на запись для монотонности.
- [ ] Тест монотонности: сто горутин пишут, дельта с любого `since` не пропускает.
- [ ] Тесты обработчика: первая синхронизация, обновление, удаление, пустая.

Вопросы себе: как ревизии могут зафиксироваться не по порядку? Что даёт блокировка и что она стоит?

🤖 Ревью. Раздел «Дельта».

## Фаза 4. Запись и защита

- [ ] `POST`, `PUT`, `DELETE` с надгробием.
- [ ] `auth.go`: Bearer, постоянное время, лимит попыток.
- [ ] Без `ADMIN_TOKEN` запись даёт 503.
- [ ] Тесты: 401, 429 после десяти, удаление появляется в дельте.

Вопросы себе: почему `==` для токена опасен? Где лимит попыток живёт и что с ним при рестарте?

## Фаза 5. Контейнеры и CI

- [ ] Compose с миграциями и импортом как отдельным сервисом или командой.
- [ ] CI как в сокращалке плюс интеграционные.
- [ ] README с примерами и описанием протокола дельты.

## Фаза 6. Клиент в календаре

- [ ] `GRDBFactStore` в `CalendarCore`: факты и ревизия.
- [ ] `FactsAPI`, `FactSyncService` с циклом `hasMore` и слиянием в транзакции на страницу.
- [ ] Тесты на фикстурах дельт: первая, обновление, удаление, обрыв.
- [ ] `RemoteFactRepository`, `BundleFallbackRepository`.
- [ ] Синк в `Task` при запуске, экран не ждёт.
- [ ] `FactRepository` не изменился. Если пришлось, записать.

Вопросы себе: почему ревизия сохраняется после каждой страницы? Что произойдёт, если слияние упало на середине страницы?

🤖 Ревью. Раздел «Клиент» и чеклист календаря целиком.

## Фаза 7. Сквозная проверка

- [ ] Сервер локально, календарь на устройстве: первая синхронизация, правка, удаление, офлайн.
- [ ] `NOTES.md`: что переехало из скелета, что изменил бы в скелете.

🤖 Финальное ревью по всему чеклисту.

## Стретч

- [ ] Пуш календарю через APNs о новой ревизии.
- [ ] Языки.
- [ ] Импорт из сборщика напрямую по URL.
