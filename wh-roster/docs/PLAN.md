# План

Фазы идут по порядку. Каждая заканчивается рабочим состоянием и коммитом. «Вопросы себе» это то, на что я должен уметь ответить после фазы, не заглядывая в документацию. Не могу ответить, значит фаза не закончена.

Где звать агента, отмечено значком 🤖. В остальных местах не звать.

## Фаза 0. Каркас

- [ ] Проект iOS, SwiftUI, Swift 6, iPhone и iPad.
- [ ] swift-format, `.gitignore`, первый коммит, папки из `ARCHITECTURE.md`.

## Фаза 1. Модель и импорт

- [ ] Модель CoreData по `SPEC.md` со связями и правилами удаления.
- [ ] `PersistenceController` с вариантом в памяти.
- [ ] `CatalogFormat`, `CatalogImporter` в фоне, пачками.
- [ ] Демо-фракция в бандле, кнопка «загрузить демо».
- [ ] Тесты импортёра: 300 отрядов, замена, дополнение, битый JSON.

Вопросы себе: что делает cascade и nullify на каждой связи? Почему импорт в фоновом контексте и что такое слияние?

🤖 Ревью. Разделы «Модель» и «Контексты».

## Фаза 2. Правила

- [ ] `RosterSnapshot`, `Snapshotting`.
- [ ] `PointsCalculator`, `RosterValidator` с тестами на все предупреждения.
- [ ] Ограничения опций: `one`, `upTo` с `max` и `perModels`.

Вопросы себе: почему правила над снимком, а не над сущностями? Что сломалось бы иначе?

## Фаза 3. Списки и юниты

- [ ] `RostersListView` с `@FetchRequest`, создание, удаление, дублирование.
- [ ] `RosterService` с группами отмены.
- [ ] `RosterView`: юниты, итог, предупреждения.
- [ ] `EntryEditorView`, `WargearPicker`.
- [ ] `DatasheetPickerView` с поиском и фильтром.
- [ ] Тесты сервиса: добавить, отменить, повторить.

Вопросы себе: как `undoManager` контекста узнаёт о моих изменениях? Что попадает в одну группу отмены?

🤖 Ревью. Раздел «Мутации и отмена».

## Фаза 4. iPad и перетаскивание

- [ ] `NavigationSplitView` с тремя колонками, обе ориентации, iPhone стеком.
- [ ] `onMove` с пересчётом `order` в одной группе отмены.
- [ ] Дублирование юнита со снаряжением.
- [ ] Заморозка имени и очков при создании юнита.

Вопросы себе: что происходит с третьей колонкой в портрете? Почему `order` пересчитывается для всех, а не для одного?

## Фаза 5. Экспорт и импорт списка

- [ ] `RosterJSON`, `fileExporter`, `fileImporter`.
- [ ] Импорт списка с отсутствующими в справочнике отрядами: «устарел».
- [ ] Тест круговой: экспорт, импорт, снимки равны.

## Фаза 6. PDF

- [ ] `PDFExporter` через `ImageRenderer` с ручной пагинацией.
- [ ] Заголовок, таблица, итог, предупреждения, заметки.
- [ ] Открыть в Файлах, распечатать.

Вопросы себе: почему `ImageRenderer` не пагинирует сам? Как разбить вью на страницы?

## Фаза 7. Миграция и края

- [ ] Новая версия модели: поле `isWarlord` у юнита. Лёгкая миграция на реальных данных.
- [ ] Тест миграции на базе старой версии из фикстур.
- [ ] Удаление справочника с сохранением списков.
- [ ] Отряд удалён из справочника: юнит «устарел», очки заморожены.

🤖 Финальное ревью по всему чеклисту.

## Стретч

- [ ] Несколько окон на iPad: два списка рядом.
- [ ] Swift Charts: очки по ключевым словам.
- [ ] `UIGraphicsPDFRenderer` с ручной вёрсткой вместо `ImageRenderer`, сравнить.
