# Страны: MVVM + Combine

Полигон 3 из 10. Одно и то же приложение «Страны», десять раскладок кода. Здесь: **MVVM с Combine, UIKit**.

Логика экрана в ViewModel, который публикует состояние и не знает, что его кто-то показывает. Контроллер подписывается на состояние и рисует. Вместо команд «покажи это» одно наблюдаемое состояние и биндинги через Combine.

## Зачем

Это самая распространённая связка в продакшн-приложениях на UIKit. Понять, что меняется по сравнению с MVP, когда презентер перестаёт командовать и начинает публиковать состояние, и что стоит Combine: подписки, потоки, утечки.

## Что хочу понять

- Combine: `@Published`, `sink`, `debounce`, `removeDuplicates`, `receive(on:)`, `AnyCancellable`.
- Одно состояние экрана как структура против нескольких `@Published`.
- Мост между `AsyncStream` из сервисов и Combine внутри ViewModel.
- Тесты ViewModel через `.values` или сбор значений `@Published`.
- Бонус: один и тот же ViewModel обслуживает UIKit-экран и SwiftUI-экран через `UIHostingController`.

## Что делает приложение

Справочник стран: список с флагом и столицей, поиск, обновление из сети, детали с кнопкой «в избранное», список избранного, кэш для запуска без сети. Полное ТЗ в `docs/SPEC.md`, оно одинаково для всех десяти полигонов.

## Готово, когда

- Три экрана работают на устройстве, запуск без сети с кэшем показывает список, избранное переживает перезапуск.
- То, что эта архитектура объявляет тестируемым (см. `docs/ARCHITECTURE.md`), покрыто тестами.
- Агент прошёл по `docs/REVIEW.md`, блокеров нет.
- В корневом индексе заполнена строка сравнения, а в `docs/NOTES.md` ответы на вопросы из раздела «Что записать в сравнение».

## Как начать

1. `docs/SPEC.md`: прочитать целиком, если это первый полигон, иначе пробежать, оно не менялось.
2. `docs/ARCHITECTURE.md`: прочитать целиком. Это единственное, что здесь отличается от соседей.
3. `docs/STACK.md`: раздел «что почитать» перед соответствующей фазой.
4. `docs/PLAN.md`: идти по фазам. `Services/` можно скопировать из предыдущего полигона.
5. `docs/NOTES.md`: по ходу записывать, что бесит и что нравится. Это и есть результат.

## Оценка

Два-три дня плюс день на бонус со SwiftUI-экраном.
